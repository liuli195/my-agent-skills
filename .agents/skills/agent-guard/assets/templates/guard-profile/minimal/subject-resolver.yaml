subject:
  identity_fields:
    - context.session_id
  required_fields:
    - context.session_id
  optional_fields:
    - context.repo
    - context.worktree
  context_sources:
    - context
    - event
  existing_match_policy: exact
  create_policy: explicit_activation_only
  ambiguous_policy: audit
