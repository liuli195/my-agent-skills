# Comet guard-readable project commands
# Keep in sync with .comet/config.yaml until the guard reads that file directly.
build_command: python plugins/test-framework/skills/test-framework/scripts/test_framework.py build --project .
verify_command: python plugins/test-framework/skills/test-framework/scripts/test_framework.py verify --project .
