# Comet guard-readable project commands
# Keep in sync with .comet/config.yaml until the guard reads that file directly.
build_command: python .build-and-verify/runtime/build_and_verify.py build --project .
verify_command: python .build-and-verify/runtime/build_and_verify.py verify --project .
