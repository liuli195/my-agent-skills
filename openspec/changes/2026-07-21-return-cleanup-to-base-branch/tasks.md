## 1. Cleanup（清理）回归测试

- [x] 1.1 先更新 `tests/test_pr_flow_cli.py` 的已合并 PR（拉取请求）cleanup（清理）场景，断言可用目标分支会被检出且当前 `HEAD`、本地目标分支与远端快照一致。
- [x] 1.2 新增目标分支被其他 worktree（工作树）占用且已同步时的场景，断言源分支仍被安全删除、当前工作树保持 detached HEAD（分离头）并记录跳过原因。
- [x] 1.3 覆盖目标分支在预检后被占用时的检出失败降级：重新读取工作树清单，已同步则完成清理，否则保留诊断并停止。
- [x] 1.4 保留并覆盖被占用目标分支落后于远端快照时在删除任一源分支前停止的安全边界。
- [x] 1.5 覆盖两个工作树同时 cleanup（清理）同一目标分支时，至多一个工作树检出该分支，另一工作树按已同步占用路径保持 detached HEAD（分离头）。
- [x] 1.6 断言缺失本地目标分支被创建后，当前工作树已检出该目标分支并记录检出完成状态。
- [x] 1.7 覆盖源分支删除失败后从已同步目标分支重试 cleanup（清理）的场景。

## 2. Cleanup（清理）实现与说明

- [x] 2.1 修改 `pr_flow.py` 的 cleanup（清理）收尾顺序：先离开源分支，再在目标分支可用时安全更新、检出并回读一致性后删除源分支；目标分支被占用时按已同步降级路径完成。
- [x] 2.2 在目标分支检出失败后重新读取工作树清单，避免解析 Git（版本管理）错误文本；在完成状态中记录检出结果、跳过原因和占用工作树，并保持现有远端快照、工作树隔离与源分支安全检查。
- [x] 2.3 更新 `pr-flow-cleanup`（拉取请求流程清理）技能，说明可用目标分支的切回行为和被占用时的 detached HEAD（分离头）降级行为。
- [x] 2.4 为同一共享 Git（版本管理）目录和目标分支的检出决定增加短生命周期锁，并在锁内重新读取占用状态、更新、检出和回读。
- [x] 2.5 仅在当前目标分支、本地引用和远端快照一致时，允许 cleanup（清理）从该目标分支继续删除未清理的源分支。
- [x] 2.6 刷新 delta spec（增量规格）以保留主规格既有 cleanup（清理）场景标题，避免归档时将其视为删除。

## 3. 验证

- [x] 3.1 运行相关 PR Flow（拉取请求流程）测试与严格 OpenSpec（开放规格）校验。
- [x] 3.2 运行仓库标准 build-and-verify（构建与验证）完整回归，并记录验证结果。

## 4. 用户授权的仓库规则

- [x] 4.1 将用户手工更新的 `AGENTS.md`（代理规则）原样纳入当前 change（变更），要求构建与验证使用 build-and-verify Skill（技能）入口。
