## 1. Tests

- [x] 1.1 Add focused regression tests for `tweak`（小改） auto-push（自动推送） and ruleset（规则集） `--auto`（自动合并） recovery.

## 2. Implementation

- [x] 2.1 Reuse existing lifecycle（生命周期） helpers so `tweak`（小改） differs from `complete`（收尾） only by skipping review gate（审查门禁）.

## 3. Verification

- [x] 3.1 Run focused PR Flow（拉取请求流程） tests and OpenSpec（开放规格） validation.
