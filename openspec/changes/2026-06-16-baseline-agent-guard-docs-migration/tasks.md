## 1. 基线变更

- [x] 1.1 创建 `openspec/config.yaml`，内容为 `schema: spec-driven`。
- [x] 1.2 创建 `baseline-agent-guard-docs-migration` 的 proposal（提案）、design（设计）、tasks（任务）和 delta spec（增量规格）文件。
- [x] 1.3 使用 OpenSpec strict validation（严格校验）验证 active change（活动变更）。

## 2. 主规格

- [x] 2.1 把 `agent-guard-core` delta requirements（增量需求）同步到 `openspec/specs/agent-guard-core/spec.md`。
- [x] 2.2 把 `agent-guard-plugin-runtime` delta requirements（增量需求）同步到 `openspec/specs/agent-guard-plugin-runtime/spec.md`。
- [x] 2.3 把 `agent-guard-brief-injection` delta requirements（增量需求）同步到 `openspec/specs/agent-guard-brief-injection/spec.md`。
- [x] 2.4 把 `agent-guard-skill-entrypoints` delta requirements（增量需求）同步到 `openspec/specs/agent-guard-skill-entrypoints/spec.md`。

## 3. 校验与归档

- [x] 3.1 使用 OpenSpec strict validation（严格校验）验证所有 main specs（主规格）。
- [x] 3.2 使用 OpenSpec strict validation（严格校验）验证所有 OpenSpec 对象。
- [x] 3.3 在 specs（规格）同步后归档 `baseline-agent-guard-docs-migration`。
