- [x] 1. 为 Global Command Guard（全局命令守卫）增加 `skip_when`（跳过条件）运行时支持。
- [x] 2. 更新 `comet-review-gate`（Comet 审查门禁）模板和用户级配置，跳过 hotfix/tweak（热修复/小改）流程。
- [x] 3. 增加回归测试并运行验证。

<!-- review skipped: requesting-code-review requires a background reviewer subagent, but this build was confirmed as executing-plans/direct on the current branch without subagent dispatch. Full local regression and user-level guard behavior checks were run instead. -->
