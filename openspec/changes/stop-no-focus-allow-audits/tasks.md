## 1. Runtime behavior

- [x] 1.1 Add regression coverage proving no-focus allow writes no audit while no-focus blocking still writes one.
- [ ] 1.2 Change the shared focus boundary so only the allow path skips audit creation and omits `audit_path`.

## 2. Contract and verification

- [ ] 2.1 Update affected Agent Guard（代理守卫）contract text and run the plugin's complete verification flow.
