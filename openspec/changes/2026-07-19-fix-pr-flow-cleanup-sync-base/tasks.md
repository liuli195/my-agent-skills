## 1. 回归保护

- [x] 1.1 更新 cleanup（清理）技能契约，并增加失败回归，覆盖本地目标分支同步、缺失分支创建、分支占用和不可快进拒绝。

## 2. 根因修复

- [x] 2.1 在 cleanup（清理）共享路径中恢复本地目标分支安全快进，同时保留 detached HEAD（分离头）和源分支安全删除行为。

## 3. 验证

- [x] 3.1 运行定向测试、真实 Git（版本管理）端到端回归、OpenSpec（开放规格）严格校验、仓库构建和快速验证。
