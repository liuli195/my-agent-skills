## 1. Manifest Version Tests

- [x] 1.1 Add failing checks that catch duplicate `PLUGIN_VERSION`（插件版本） constants in plugin package tests.
- [x] 1.2 Update `pr-flow`（拉取请求流程） and `build-and-verify`（构建与验证） manifest（清单） tests to read versions from manifest（清单） files.
- [x] 1.3 Verify focused package tests and OpenSpec（开放规格） validation.
