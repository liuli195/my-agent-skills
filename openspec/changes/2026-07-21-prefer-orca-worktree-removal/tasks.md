## 1. Orca（工作区管理器）选择回归

- [x] 1.1 在 `tests/test_pr_flow_cli.py` 先覆盖 Orca（工作区管理器）按规范化路径匹配时使用非强制 Orca（工作区管理器）删除、且不调用 Git（版本管理）删除的场景。
- [x] 1.2 覆盖 Orca（工作区管理器）未匹配、命令不可用或无效 JSON（数据格式）时回退非强制 Git（版本管理）删除的场景。
- [x] 1.3 覆盖 Orca（工作区管理器）已匹配但删除失败时输出 `orca_worktree_remove_failed`（Orca 工作树删除失败）、保留诊断且不回退 Git（版本管理）删除的场景。

## 2. 删除器实现

- [x] 2.1 在 `pr_flow.py` 添加最小 Orca（工作区管理器）命令解析和工作树列表探测，保持 Orca（工作区管理器）不可用时的 Git（版本管理）兼容性。
- [x] 2.2 将 `remove_worktree()`（删除工作树）接入 Orca（工作区管理器）优先删除、失败阻断和既有 Git（版本管理）回读确认，保留主工作树、脏工作树与无强制删除边界。
- [x] 2.3 修复完整 PR Flow（拉取请求流程）并发回归发现的 Windows（视窗）锁初始化竞态：在共享锁取得后才写入首字节，并保留既有锁语义。

## 3. 用户说明

- [x] 3.1 更新 `pr-flow-cleanup`（拉取请求流程清理）、`pr-flow-complete`（拉取请求流程收尾）和 `pr-flow-hotfix`（拉取请求流程热修复）技能，说明 Orca（工作区管理器）已登记工作树的优先删除与失败行为。

## 4. 验证

- [x] 4.1 运行相关 PR Flow（拉取请求流程）回归和严格 OpenSpec（开放规格）校验。
- [x] 4.2 运行仓库标准 build-and-verify（构建与验证）检查，并记录本 change（变更）的验证结果。
