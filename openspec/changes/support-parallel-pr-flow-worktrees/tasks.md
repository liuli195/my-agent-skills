## 1. 工作树运行隔离

- [x] 1.1 改造 `tests/test_pr_flow_cli.py` 现有状态测试，覆盖流程上下文、工作树路径、固定修改锁、竞争修改命令不改写运行状态与 `.pr-flow/last-status.json`，以及 diagnose（诊断）只读输出 actor（执行者）和重试命令
- [x] 1.2 复用现有 Git（版本控制）封装、`write_status`、下一步命令和 actor（执行者）能力，实现工作树上下文、按源分支保存的 `.pr-flow/runs/` 状态、兼容状态输出和每工作树固定操作系统文件锁

## 2. 最新基线与合并门禁

- [x] 2.1 将现有 hotfix（热修复）远端读取逻辑提取为单一快照函数，复用于自动推送及 PR（拉取请求）远端修改前的目标提交读取与祖先判断，统一输出 `base_outdated` 并断言停止时没有远端修改
- [x] 2.2 改造现有等待、失败和源提交变化测试，只新增必需检查为空、缺失、过期及目标提交变化场景
- [x] 2.3 改造现有 `pr_checks`、`wait_for_checks` 和 `merge_pr`，实现非空必需检查校验及目标提交快照与失效复核

## 3. 热修复竞争

- [x] 3.1 复用远端提交快照函数，增加 hotfix（热修复）验证期间目标分支推进测试并实现推送前复核

## 4. 工作树安全清理

- [x] 4.1 为 cleanup（清理）、complete（完整流程）、tweak（小改）和 hotfix（热修复）接入 `--remove-worktree`（删除工作树参数）；complete（完整流程）和 tweak（小改）向内部 PR cleanup（拉取请求清理）传播，hotfix（热修复）在推送回读成功后直接进入共享工作树删除路径且不查询 PR（拉取请求）；同时实现清理预检和最新远端目标提交 detached HEAD（分离头），删除旧的步骤检查点与禁止重跑分支，按实时 Git（版本控制）状态幂等删除分支
- [x] 4.2 改造现有完整流程、脏工作树和部分失败测试，只新增主工作树、外部 `--project`（项目路径参数）和 hotfix（热修复）推送回读后不查询 PR（拉取请求）的同步删除场景
- [x] 4.3 使用原生 `git worktree remove` 且不传 `--force`（强制参数），删除后回读工作树清单

## 5. 集成验证与说明

- [ ] 5.1 扩展现有 `init_complete_project` 裸仓库夹具，使用两个 linked worktree（关联工作树）和并发子进程完成并行 PR（拉取请求）、目标推进、冲突、一方失败、同一源分支被另一工作树占用及安全清理端到端回归
- [x] 5.2 更新 PR Flow（拉取请求流程）相关 Skill（技能）说明，覆盖默认清理、显式工作树删除和恢复命令
- [ ] 5.3 运行 PR Flow（拉取请求流程）定向测试、Plugin（插件）打包测试、仓库完整端到端验证及 OpenSpec（开放规格）严格校验
