## 1. 回归

- [x] 1.1 为终态和活动 PR（拉取请求）补充 `complete`（收尾）与 `tweak`（小改）回归，并确认新增断言在当前实现下失败。

## 2. 修复

- [x] 2.1 在 `find_pr()`（PR 查询函数）中仅保留 `OPEN`（未合并）PR（拉取请求）作为活动 PR（拉取请求），并使回归通过。

## 3. 验证

- [x] 3.1 运行目标回归和仓库完整集成验证（E2E），确认终态 PR（拉取请求）的旧源提交不再参与基线校验。
