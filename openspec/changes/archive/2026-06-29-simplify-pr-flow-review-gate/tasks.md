## 1. Runtime

- [x] 1.1 Restrict PR Flow（拉取请求流程）review gate（审查门禁） validation and complete（收尾） runtime to `github`（GitHub 审查） and `skip`（跳过）.
- [x] 1.2 Remove PR Flow（拉取请求流程）local review evidence（本地审查证据） config and tests.

## 2. Init Guidance

- [x] 2.1 Update `pr-flow-init`（初始化）questionnaire（问答模板） and draft guidance（草案规则） so branch protection（分支保护） derives `github`（GitHub 审查） or `skip`（跳过）.

## 3. Verification

- [x] 3.1 Run focused PR Flow（拉取请求流程）tests and OpenSpec（开放规格） validation for `simplify-pr-flow-review-gate`.
