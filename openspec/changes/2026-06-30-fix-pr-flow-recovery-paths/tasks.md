## 1. PR Flow recovery behavior

- [x] 1.1 Add failing tests for transient `gh pr view` retry, pending-check merge recovery, deprecated `evidencePath` warning, and `--fixes` append behavior.
- [x] 1.2 Implement minimal PR Flow changes using existing helpers and standard library only.
- [x] 1.3 Run focused tests, full PR Flow CLI tests, OpenSpec validation, and fast repository verification.
