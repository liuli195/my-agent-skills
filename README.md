# my-agent-skills

用户级 Agent Skill 源码仓库。

当前只初始化 `agent-guard` 的仓库骨架：

- `.agents/skills/agent-guard/`：Skill 源码。
- `docs/`：仓库规则和架构决策占位。
- `scripts/install/`：安装脚本占位。

当前不会执行：

- 安装 Skill 到用户目录。
- 安装 Codex 或 Git hook。
- 启用阻断模式。
- 初始化目标项目守卫。
