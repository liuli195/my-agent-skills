hook_bindings:
  - id: manual-close
    source: manual
    trigger_event: state_completed
    event_type: state_completed
    target_profile: minimal-sample
    install:
      status: not_installed
      target: manual
      rollback: no installed hook
    transitions:
      - close_after_note
    guard_points:
      - completion_note_present
