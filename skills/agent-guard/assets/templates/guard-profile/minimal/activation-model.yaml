activation:
  allowed_sources:
    - agent-guard-skill
    - manual
  required_profile_ref: true
  scopes:
    - project
  on_existing_instance: select
  on_missing_instance: create
  initial_state: open
