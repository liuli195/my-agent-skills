target:
  id: minimal-sample-target
  type: temporary_task
  name: 最小样例任务
  source: bundled-template
  boundary: 只演示 Guard Profile（守卫画像）连接，不编码真实 workflow（工作流）。
  goals:
    - 展示显式激活和单个关闭状态转换。
  allowed_actions:
    - 显式激活 Guard Profile（守卫画像）。
    - 必需 note（说明记录）存在后关闭画像。
  forbidden_actions:
    - 把此样例当作生产业务规则。
