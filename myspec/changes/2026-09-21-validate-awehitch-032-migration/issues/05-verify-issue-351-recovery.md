# 05: 验证 Issue 351 恢复行为

**What to build:** 使用一次性任务和对话验证远端错误、操作竞争、进程恢复、重复发送和超时语义；对失败项按成本收益决定最小补丁或上游反馈。

**Blocked by:** 04: 按实测结果改造并应用补丁。

**Status:** partially-verified

- [ ] 远端错误分类尚未完成受控实测；当前调用规则仅规定错误和超时不得触发重建。
- [ ] `connector-setup` 与 `open_chat` 的高级操作竞争尚未完成受控实测；本轮不据此应用补丁 6。
- [ ] 重复 BOOT/INIT 的幂等性尚未完成受控实测；当前调用规则禁止在结果不明时重复发送。
- [x] 实测能跨进程恢复原任务对应的对话，但新进程读取新回复仍持续超时；该高成本限制已记录为上游项，本轮不新增本地状态基础设施。
