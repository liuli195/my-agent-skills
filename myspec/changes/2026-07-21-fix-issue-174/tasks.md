## 1. Agent Guard Pi 适配

- [x] 1.1 增加 Pi（编码助手）专属 Agent Guard（代理守卫）extension（扩展），以 Pi（编码助手）原生会话和工具调用事件调用既有 Hook Router（钩子路由器）。
- [x] 1.2 以最小共享改动支持 `source=pi`（来源），复用现有 Guard Profile（守卫画像）、Session Focus（会话焦点）、命令提取、allow（允许）/deny（拒绝）和审计语义。
- [x] 1.3 补齐 Pi（编码助手）适配回归测试，并断言 Codex（编码助手）/Claude（克劳德）Hook（钩子）清单、运行路径和现有测试保持兼容。
- [x] 1.4 断言 Pi（编码助手）适配不导入、配置、调用或修改其他 Pi（编码助手）插件。

## 2. PR Flow 可移植执行入口

- [x] 2.1 增加 Pi（编码助手）专属 PR Flow（拉取请求流程）extension（扩展）入口，从已安装包自身位置定位 `pr_flow.py`（PR Flow 脚本）。
- [x] 2.2 更新 PR Flow Skill（拉取请求流程技能）路由，使 Pi（编码助手）使用该入口而 Codex（编码助手）/Claude（克劳德）保留现有已安装脚本调用方式。
- [x] 2.3 让 `pr_flow.py`（PR Flow 脚本）从自身位置生成可执行且安全引用的恢复命令，移除源码仓库相对路径假设。
- [x] 2.4 补齐已安装插件和外部目标仓库路径解析的回归测试，并保留源码仓库维护者示例。

## 3. 端到端验证

- [x] 3.1 在两个目标仓库中从 Pi（编码助手）技能入口验证 PR Flow（拉取请求流程）脚本解析和恢复命令。
- [x] 3.2 在 Pi（编码助手）中验证 Agent Guard（代理守卫）自动工具调用拦截与既有 Guard Profile（守卫画像）行为。
- [x] 3.3 运行仓库完整构建与端到端回归，记录 Codex（编码助手）/Claude（克劳德）兼容性证据。
