# 03 — 引导词压到能发出去

**What to build:** 新会话开始时，ChatGPT 收到的角色设定**完整且一致**，并且**真的发得出去**。当前引导词 1361 字节、不以 `[C2C]` 开头，而唯一能发任意文本的通道两条都硬性要求——所以它永远发不出去，每个 Agent（代理）只能临场自己压一版。本票据把引导词压到只保留**能改变 ChatGPT 行为**的信息：1361 → 816 字节，余量 208 字节。

**Blocked by:** 02（同一个模板文件 + 同一个测试文件）

**Status:** ready-for-agent

- [x] 渲染后的引导词以 `[C2C]` 开头。
- [x] 按**最坏长度**连接器名替换后，引导词字节数 **≤ 1024**（断言落在**渲染输出**上）：实际名 816 字节、最坏名（40 字符）841 字节。
- [x] 渲染后的**工作区校验词**以 `[C2C]` 开头（共两处，均已加头并经断言覆盖）。
- [x] **连接器名保留**在引导词内。
- [x] 已删除不改变任何决策的表述：「只检查需要的文件」「避免不必要的重写」「要有实质内容」；同类项已合并。
- [x] 实际发送一次引导词，确认发送通道接受（返回成功）。
- [x] **最终真实入口冒烟**：跑一遍补丁脚本 → 真连一次（开对话 → 发引导词 → 收到回复）。
      证据见 `../verification.md`：ChatGPT 报回的 `HEAD` 与本地一致，并按 C2C 格式回话。
- [x] 红灯证据与绿灯证据来自**同一入口**（渲染后的技能文本）：红灯 `assert boot[0].startswith("[C2C]")` → 绿灯 `3 passed`。
- [x] 改动以本地补丁形式进入 `scripts/awehitch-local-patches.ps1`。
- [x] 一个提交（`7f89fec2`）。

**Spec reference:** `myspec/changes/2026-09-18-fix-awehitch-reconnect-and-boot-prompt/spec.md`
