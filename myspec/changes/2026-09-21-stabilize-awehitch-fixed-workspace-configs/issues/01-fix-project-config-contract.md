# 01: 固定项目配置并强化接入检查

**What to build:** 让三个仓库的固定项目配置都能直接启动正确工作区的 Codex 控制面，进入 `codex` 浏览器槽位池并共享三个并发槽位；补丁脚本能够识别完整配置并继续保护用户级配置不被改写。

**Blocked by:** None (can start immediately).

**Status:** done

- [x] 三份主分支配置分别保留正确工作区并包含 `--harness codex` 和三槽环境值。
- [x] 用户级 Codex 配置在运行接入命令前后逐字节不变。
- [x] 错误工作区、缺 harness、缺槽位值和不兼容字段均被诊断为未正确接入。
- [x] 隔离测试证明补丁脚本首次安装成功且第二次运行不重写目标。
