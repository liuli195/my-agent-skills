# 03: 托管授权选择器与安全观测

**What to build:** 让唯一补丁入口能够安全恢复已验证的授权按钮选择器，并在授权失败时留下足以区分各阶段的非敏感日志，而不改变授权流程的等待、重试和成功判定。

**Blocked by:** 01: 固定项目配置并强化接入检查。

**Status:** done

- [x] 选择器字段缺失时合并、相同时跳过、人工差异或损坏文件时停止且不写入。
- [x] 未知选择器字段保持不变。
- [x] 日志覆盖选择器加载、连接点击、登录按钮状态与点击、授权页出现和最终授权验证阶段。
- [x] 日志不含配对码、令牌、页面内容、原始错误文本或带参数地址。
- [x] 未完成 stash 仅选择性移植，不整体应用或删除。
