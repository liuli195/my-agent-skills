# 02: 让正常使用流程先检查状态

**What to build:** 让快速问答和开发任务在全机服务健康时直接使用现有服务，只在服务停止、工作区未登记或只读诊断明确要求时运行启动或连接器修复命令。

**Blocked by:** 01: 固定项目配置并强化接入检查。

**Status:** done

- [x] 渲染后的 Skill 不在正常入口无条件运行 `up`。
- [x] 状态检查使用实际支持的命令形状，诊断默认使用不修复模式。
- [x] 服务停止、工作区未登记和连接器明确损坏各自保留可执行恢复路径。
- [x] 控制消息仍满足 `[C2C]` 格式和 1024 字节限制。
