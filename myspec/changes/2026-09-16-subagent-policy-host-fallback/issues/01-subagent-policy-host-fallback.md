# 01 — 子代理策略回退改为宿主默认承载同一角色

**What to build:** 宿主无法提供四个角色指定的模型或思考强度时，子代理策略不再放弃子代理，而是回退到宿主默认的子代理模型和思考强度，**承载同一个角色**；四个角色、职责与读写边界不因回退改变，回退在提示词和报告中显式声明。开发流程的参考文档同步指向该回退规则。既有的 `subagent-policy` 能力规格在交付阶段同步更新，消除技能与规格的矛盾。技能正文保持宿主中立，不点名任何宿主。

**Blocked by:** None — can start immediately

**Status:** ready-for-agent

- [x] 宿主无法提供指定模型或思考强度时，回退到宿主默认的子代理模型与思考强度，并承载同一角色。
- [x] 四个角色及其职责、读写边界不因回退改变；不得因模型不可用而跳过角色，也不得改用主 Agent（代理）自行完成。
- [ ] 回退在**委派提示词与结果报告**中显式声明；既有的「不以其他配置冒充该角色」禁止性措辞保留。
      （限定审查判定为部分完成：当前只在「角色契约」段声明，`## 委派提示词` 的必备清单与 `## 结果验收` 未列入。）
- [x] 技能正文保持宿主中立，不出现具体宿主名称；技能仍为单文件，顶层小节保持原样。
- [x] 开发流程参考文档在提到固定模型与思考强度时，指向该技能的回退规则。
- [x] 既有 `tests/subagent_policy.test.mjs` 与 `tests/dev_flow.test.mjs` 全部通过，不新增测试接缝。
- [ ] 既有能力规格 `myspec/specs/subagent-policy/spec.md` 在**交付阶段**经 `my-spec-add` 更新为新回退语义，与技能不再矛盾。
      （实施阶段不得修改或提交 `myspec/specs/`，故该条只能在门禁二授权后落地。）

**Spec reference:** `myspec/changes/2026-09-16-subagent-policy-host-fallback/spec.md`
