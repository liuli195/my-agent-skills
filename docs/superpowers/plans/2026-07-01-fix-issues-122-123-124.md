---
change: fix-issues-122-123-124
design-doc: docs/superpowers/specs/2026-07-01-fix-issues-122-123-124-design.md
base-ref: f8a35be717bbe75fec4508a0601e9994bca8aaea
---

# Fix Issues 122 123 124 Implementation Plan（实施计划）

> **For agentic workers（代理工作者）:** REQUIRED SUB-SKILL（必需子技能）: Use superpowers:subagent-driven-development（子代理驱动开发） or superpowers:executing-plans（执行计划） to implement this plan task-by-task（逐项执行）。Steps（步骤） use checkbox（复选框） syntax（语法） for tracking（跟踪）。

**Goal（目标）:** 用最小改动修复 #122、#123、#124，不引入新依赖，不保留可要可不要的兼容入口。

**Architecture（架构）:** 三个 issue（问题）相互独立，按风险从低到高执行：先删测试专用生产 CLI（命令行接口），再升级 GitHub Actions（GitHub 自动化）引用，最后调整 PR Flow（拉取请求流程）诊断分流。测试只改现有测试文件，生产逻辑只改对应入口文件和 workflow（工作流）文件。

**Tech Stack（技术栈）:** Python（Python 语言）、pytest（Python 测试框架）、argparse（命令行参数解析）、GitHub Actions（GitHub 自动化）、YAML（配置格式）、OpenSpec（开放规格）。

---

## File Structure（文件结构）

- Modify（修改）: `plugins/cross-agent-review/skills/cross-agent-review/scripts/cross_agent_review.py`
  - 删除 production CLI（生产命令行）的 `--fake-reviewer-results`，删除 `ReviewInput.fake_reviewer_results`，删除 fake reviewer（伪审查者）解析和 dispatch（派发）绕过。
- Modify（修改）: `tests/test_cross_agent_review_cli.py`
  - 把成功路径测试改成 in-process monkeypatch（进程内猴子补丁）`dispatch_reviewers`，新增 production CLI（生产命令行）拒绝旧参数的回归测试。
- Modify（修改）: `.github/workflows/release.yml`
  - `actions/checkout@v4` 升到 `actions/checkout@v5`。
- Modify（修改）: `.github/workflows/full-verify.yml`
  - `actions/checkout@v4` 升到 `actions/checkout@v5`。
  - `actions/setup-node@v4` 升到 `actions/setup-node@v6`。
  - `node-version: "20"` 升到 `node-version: "24"`。
  - `actions/setup-python@v5` 升到 `actions/setup-python@v6`。
- Modify（修改）: `.github/workflows/codeql.yml`
  - `actions/checkout@v4` 升到 `actions/checkout@v5`。
  - 保留 `github/codeql-action/*@v4`，因为 CodeQL（代码扫描）当前大版本仍是 v4。
- Modify（修改）: `plugins/release-flow/skills/release-flow/assets/templates/github/workflows/release.yml`
  - `actions/checkout@v4` 升到 `actions/checkout@v5`。
- Modify（修改）: `tests/test_release_flow_cli.py`
  - 扫描所有 active workflow（活动工作流）和 release-flow（发布流程）模板，断言旧 Node.js 20（Node 运行时）相关引用不存在，CodeQL（代码扫描）v4 是明确例外。
- Modify（修改）: `plugins/pr-flow/skills/pr-flow/scripts/pr_flow.py`
  - `diagnose`（诊断）在 missing upstream（缺少上游分支）时输出 `DISPATCH_REQUIRED`，并用现有 `pr_body_next_command("complete", ...)` 指向 `complete`（完成）。
- Modify（修改）: `tests/test_pr_flow_cli.py`
  - 更新 missing upstream（缺少上游分支）测试，保持 new PR（新拉取请求）和 existing upstream（已有上游分支）路径不回退。

## Task 1: #122 Remove Fake Reviewer Production CLI（删除伪审查生产命令行入口）

**Files（文件）:**
- Modify（修改）: `plugins/cross-agent-review/skills/cross-agent-review/scripts/cross_agent_review.py`
- Modify（修改）: `tests/test_cross_agent_review_cli.py`

- [x] **Step 1: Add failing CLI regression test（添加失败回归测试）**

Add this test near `test_run_rejects_legacy_tests_file_argument`:

```python
def test_run_rejects_fake_reviewer_results_argument(tmp_path: Path) -> None:
    project = tmp_path / "repo"
    init_repo(project)
    head = commit_review_context(project)
    input_file = write_review_input(project, head, head)
    output_dir = input_file.parent.parent

    result = run(
        "run",
        "--input-file",
        str(input_file),
        "--fake-reviewer-results",
        "[]",
        cwd=project,
    )

    assert result.returncode == 2
    assert "unrecognized arguments: --fake-reviewer-results" in result.stderr
    assert not (output_dir / "review-report.md").exists()
```

- [x] **Step 2: Run the new test and verify it fails（运行新测试并确认失败）**

Run:

```powershell
python -m pytest -q tests/test_cross_agent_review_cli.py::test_run_rejects_fake_reviewer_results_argument
```

Expected（预期）: FAIL（失败），因为当前 production CLI（生产命令行）仍接受 `--fake-reviewer-results`。

- [x] **Step 3: Replace fake CLI success tests with monkeypatch（用猴子补丁替代伪命令行成功测试）**

Add one helper in `tests/test_cross_agent_review_cli.py` and use it only for tests that need a successful review without real SDK（软件开发工具包）dispatch（派发）:

```python
def run_review_in_process(module, monkeypatch, project: Path, *args: str) -> int:
    monkeypatch.chdir(project)
    monkeypatch.setattr(module, "resolve_sdk_python", lambda explicit, require_real_sdk: "fake-sdk")
    monkeypatch.setattr(
        module,
        "dispatch_reviewers",
        lambda review_args, sdk_python: [
            module.markdown_review(role, f"# Review Result: {role}\n## Findings\nNo findings.")
            for role in module.REVIEWER_ROLES
        ],
    )
    return module.main(list(args))
```

If the test file does not already have a module loader, add this local helper:

```python
def load_cross_agent_review_module():
    spec = importlib.util.spec_from_file_location("cross_agent_review_under_test", SCRIPT)
    assert spec is not None
    assert spec.loader is not None
    module = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(module)
    return module
```

Update success-path tests such as `test_input_files_in_space_directory_are_allowed`, `test_fake_reviewer_results_bypass_real_sdk_for_tests`, and `test_default_run_does_not_archive_context_snapshots_or_git_manifest` so they call:

```python
module = load_cross_agent_review_module()
status = run_review_in_process(module, monkeypatch, project, "run", "--input-file", str(input_file))
captured = capsys.readouterr()

assert status == 0
assert "status: review_ready" in captured.out
```

Acceptance（验收）: no test passes `--fake-reviewer-results`; tests that need reviewer（审查者）output（输出） use in-process monkeypatch（进程内猴子补丁）。

- [x] **Step 4: Delete fake production support（删除伪结果生产支持）**

In `cross_agent_review.py`, remove these production pieces:

```python
fake_reviewer_results: str | None
```

```python
def no_blocking_review(role: str) -> dict:
    ...


def load_fake_reviewer_results(raw: str | None) -> list[dict]:
    ...
```

Remove the parser argument:

```python
run_parser.add_argument("--fake-reviewer-results")
```

Make `dispatch_reviewers` direct:

```python
def dispatch_reviewers(review_args: ReviewInput, sdk_python: str) -> list[dict]:
    return run_sdk_dispatch_subprocess(review_args, sdk_python)
```

Make SDK（软件开发工具包）resolution always require a real SDK（软件开发工具包）for production `run`:

```python
sdk_python = resolve_sdk_python(review_args.sdk_python, require_real_sdk=True)
```

Acceptance（验收）: `ReviewInput` has no `fake_reviewer_results`; production code has no `load_fake_reviewer_results`, no `no_blocking_review`, no fake dispatch（伪派发）branch（分支）, and no `--fake-reviewer-results` parser argument.

- [x] **Step 5: Run focused #122 tests（运行 #122 聚焦测试）**

Run:

```powershell
python -m pytest -q tests/test_cross_agent_review_cli.py::test_run_rejects_fake_reviewer_results_argument tests/test_cross_agent_review_cli.py::test_input_files_in_space_directory_are_allowed tests/test_cross_agent_review_cli.py::test_default_run_does_not_archive_context_snapshots_or_git_manifest tests/test_cross_agent_review_cli.py::test_sdk_missing_reports_clear_error
```

Expected（预期）: PASS（通过）。

## Task 2: #123 Upgrade Workflow Action Runtime References（升级工作流动作和运行时引用）

**Files（文件）:**
- Modify（修改）: `.github/workflows/release.yml`
- Modify（修改）: `.github/workflows/full-verify.yml`
- Modify（修改）: `.github/workflows/codeql.yml`
- Modify（修改）: `plugins/release-flow/skills/release-flow/assets/templates/github/workflows/release.yml`
- Modify（修改）: `tests/test_release_flow_cli.py`

- [x] **Step 1: Add workflow scan regression（添加工作流扫描回归测试）**

Add this test next to `test_workflows_are_thin_entrypoints`:

```python
def test_workflows_use_current_low_risk_action_versions() -> None:
    workflow_paths = [
        *sorted((REPO_ROOT / ".github" / "workflows").glob("*.yml")),
        REPO_ROOT
        / "plugins"
        / "release-flow"
        / "skills"
        / "release-flow"
        / "assets"
        / "templates"
        / "github"
        / "workflows"
        / "release.yml",
    ]
    workflows = {path: path.read_text(encoding="utf-8") for path in workflow_paths}
    combined = "\n".join(workflows.values())

    assert "actions/checkout@v4" not in combined
    assert "actions/setup-node@v4" not in combined
    assert 'node-version: "20"' not in combined
    assert "actions/setup-python@v5" not in combined

    codeql_workflow = workflows[REPO_ROOT / ".github" / "workflows" / "codeql.yml"]
    assert "github/codeql-action/init@v4" in codeql_workflow
    assert "github/codeql-action/analyze@v4" in codeql_workflow
```

- [x] **Step 2: Run the workflow test and verify it fails（运行工作流测试并确认失败）**

Run:

```powershell
python -m pytest -q tests/test_release_flow_cli.py::test_workflows_use_current_low_risk_action_versions
```

Expected（预期）: FAIL（失败），because（因为） current workflow（当前工作流） still contains old references（旧引用）。

- [x] **Step 3: Apply only low-risk replacements（只做低风险替换）**

Make these exact YAML（配置格式） replacements:

```text
.github/workflows/release.yml
  actions/checkout@v4 -> actions/checkout@v5

.github/workflows/full-verify.yml
  actions/checkout@v4 -> actions/checkout@v5
  actions/setup-node@v4 -> actions/setup-node@v6
  node-version: "20" -> node-version: "24"
  actions/setup-python@v5 -> actions/setup-python@v6

.github/workflows/codeql.yml
  actions/checkout@v4 -> actions/checkout@v5
  github/codeql-action/init@v4 stays unchanged（保持不变）
  github/codeql-action/analyze@v4 stays unchanged（保持不变）

plugins/release-flow/skills/release-flow/assets/templates/github/workflows/release.yml
  actions/checkout@v4 -> actions/checkout@v5
```

Acceptance（验收）: all active workflow（活动工作流） files and release-flow（发布流程）template（模板） are scanned; only CodeQL（代码扫描）v4 remains as the explicit exception（明确例外）。

- [x] **Step 4: Run focused #123 tests（运行 #123 聚焦测试）**

Run:

```powershell
python -m pytest -q tests/test_release_flow_cli.py::test_workflows_are_thin_entrypoints tests/test_release_flow_cli.py::test_workflows_use_current_low_risk_action_versions
```

Expected（预期）: PASS（通过）。

## Task 3: #124 Point Missing Upstream Diagnose To Complete（让缺少上游分支诊断指向完成）

**Files（文件）:**
- Modify（修改）: `plugins/pr-flow/skills/pr-flow/scripts/pr_flow.py`
- Modify（修改）: `tests/test_pr_flow_cli.py`

- [x] **Step 1: Update the missing upstream test first（先改缺上游分支测试）**

Rename or replace the existing missing upstream（缺少上游分支）test with:

```python
def test_diagnose_outputs_dispatch_required_without_upstream(tmp_path: Path) -> None:
    project = tmp_path / "repo"
    init_repo(project)
    write_confirmed_pr_flow_config(project)
    init_feature_branch(project)

    result = run("diagnose", "--project", str(project))

    assert result.returncode == 1
    assert "DISPATCH_REQUIRED" in result.stdout

    status = json.loads((project / ".pr-flow" / "last-status.json").read_text(encoding="utf-8"))
    assert status["status"] == "DISPATCH_REQUIRED"
    assert status["command"] == "diagnose"
    assert status["details"]["reason"] == "missing_upstream"
    assert status["details"]["branch"] == "feature/demo"
    assert status["details"]["baseBranch"] == "main"
    assert " complete " in f" {status['details']['nextCommand']} "
    assert "--summary" in status["details"]["nextCommand"]
    assert "--scope" in status["details"]["nextCommand"]
```

- [x] **Step 2: Run the missing upstream test and verify it fails（运行缺上游分支测试并确认失败）**

Run:

```powershell
python -m pytest -q tests/test_pr_flow_cli.py::test_diagnose_outputs_dispatch_required_without_upstream
```

Expected（预期）: FAIL（失败），because（因为） current diagnose（当前诊断） still returns `PUSH_REQUIRED` or points to manual push（手工推送）。

- [x] **Step 3: Move missing upstream handling after status check（把缺上游处理移到状态检查之后）**

In `run_diagnose`, keep `upstream_result` and `upstream`, then run `git status --short` before the missing upstream（缺少上游分支）stop（停止状态）。Use this target block:

```python
    status_result = git(project, "status", "--short")
    if status_result.returncode != 0:
        details = command_failure_details("git_status_failed", status_result)
        details["branch"] = branch
        details["baseBranch"] = base_branch
        details["upstream"] = upstream
        return stop(project, args.command, "EXCEPTION_REQUIRED", details["reason"], details)
    dirty = status_result.stdout.strip()

    if upstream_result.returncode != 0 and branch != base_branch:
        details = command_failure_details("missing_upstream", upstream_result)
        details.update(
            {
                "branch": branch,
                "baseBranch": base_branch,
                "upstream": "",
                "dirty": dirty,
                "reason": "missing_upstream",
                "nextCommand": pr_body_next_command(
                    "complete",
                    project,
                    argparse.Namespace(summary="", scope="", fixes=[]),
                ),
                "optionalFixesArg": "--fixes 98",
            }
        )
        return stop(project, args.command, "DISPATCH_REQUIRED", "missing_upstream", details)
```

Delete the old early return:

```python
return stop(project, args.command, "PUSH_REQUIRED", "push current branch before continuing", details)
```

Acceptance（验收）: `diagnose`（诊断） preserves（保留） missing upstream（缺少上游分支）事实 but routes（分流） to `complete`（完成）, which already owns safe auto-push（安全自动推送）。

- [x] **Step 4: Run focused #124 tests（运行 #124 聚焦测试）**

Run:

```powershell
python -m pytest -q tests/test_pr_flow_cli.py::test_diagnose_outputs_dispatch_required_without_upstream tests/test_pr_flow_cli.py::test_diagnose_on_feature_branch_without_pr_reports_dispatch_required
```

Expected（预期）: PASS（通过）。

## Task 4: Final Verification（最终验证）

**Files（文件）:**
- Verify（验证） only; no new production files（生产文件）.

- [x] **Step 1: Run focused suites（运行聚焦套件）**

Run:

```powershell
python -m pytest -q tests/test_cross_agent_review_cli.py tests/test_release_flow_cli.py tests/test_pr_flow_cli.py
```

Expected（预期）: PASS（通过）。

- [x] **Step 2: Run OpenSpec validation（运行开放规格验证）**

Run:

```powershell
openspec validate fix-issues-122-123-124 --strict
```

Expected（预期）: PASS（通过）。

- [x] **Step 3: Acceptance checklist（验收清单）**

Confirm（确认）:

```text
#122
- cross_agent_review.py has no --fake-reviewer-results production CLI.
- cross_agent_review.py has no fake_reviewer_results field.
- Tests use monkeypatch for reviewer output injection.

#123
- .github/workflows/*.yml and release-flow template are scanned by tests.
- checkout/setup-node/setup-python/node-version references are upgraded where listed.
- CodeQL v4 remains and is asserted as the explicit exception.

#124
- diagnose missing upstream writes status DISPATCH_REQUIRED.
- details.reason is missing_upstream.
- details.nextCommand points to complete and includes --summary and --scope.
```
