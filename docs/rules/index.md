# Rules Index

- Maintain user-level Skill source in this repository.
- Do not install user-level Skills without explicit user authorization.
- Do not create or modify target project hooks without explicit user authorization.
- Do not configure state permissions that can return `deny` without explicit user authorization.
- Keep concrete workflow rules in Guard Profile, not in shared Runtime or hooks.
