# 生命周期命令隔离 Orca 临时 Codex 配置

**Status（状态）：** accepted

生命周期命令不把 Orca（开发环境）的临时 Codex（代码代理）配置目录当作用户客户端目标：显式 `--codex-home` 优先，识别到 Orca 临时目录时默认使用用户 Codex 配置目录，其他自定义目录继续生效。`update`（更新）发现旧来源时先阻断并给出精确迁移命令，而不是隐式修改多个客户端；这样可以保持会话隔离，避免在错误目录读写或在用户未明确迁移时改变全局客户端状态。
